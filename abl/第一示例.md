1，数据库表
表ocrd:

   card_code

   car_name

   slp_code

   u_ext_slp_code

表olsp:

  slp_code

  u_is_ext_sale

  u_email

  slp_name

表dln1:

   u_sub_card_code

   u_sub_card_name

   u_frgn_name

   u_opn

表sys_user:

   email

2,角色

普通业务员 

表有sale_email字段，sale_email=\#currentUser.email 

表没有sale_email字段，那就要用join表，相对复杂一点。类似这样：EXISTS (SELECT 1 FROM sys_user u WHERE u.username = {alias}.inside_saler AND u.region IN ( #{#currentUser.regions} ))  有些用户信息表，都得用redis缓存。

业务员主管

无权限

普通外勤 

表有ext_sale_email字段，ext_sale_email=\#currentUser.email 同上

外勤主管





